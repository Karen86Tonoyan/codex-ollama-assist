# apps/

- `web-assistant/` — Page Assist (browser extension / web UI for local models)
- `ollama-app/` — Vite dashboard talking to `http://127.0.0.1:11434`

Install separately (different package managers). Do not hoist into a single workspace.
